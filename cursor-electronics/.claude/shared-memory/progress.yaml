# progress.yaml
# LAYER 4 (Progress) — module-level reality for Circuit OS
# Last updated: 2026-06-02 (manually — regen_state.py needs to be updated for real backend)
#
# Status legend:
#   verified_done  ✅  module exists + all tests pass
#   untested       ⚠️   module exists + no dedicated unit test (works in integration)
#   broken         ❌  module exists + test fails
#   not_started    ⬜  module in plan but not built

_note: "Updated 2026-06-02. Reflects actual Circuit OS backend (not sample data)."
_layer: "LAYER 4 (Progress) — module-level reality"
generated_at: "2026-06-02T12:00:00Z"
project: "Circuit OS"

# ─── CORE ────────────────────────────────────────────────────────────────────

files:

  backend/core/ir_schema.py:
    status: verified_done
    description: "Locked CircuitIR Pydantic model — the central contract"
    tests: "tests/test_ir_schema.py — 20 tests, all pass"
    key_classes:
      CircuitIR: {status: verified_done}
      Component: {status: verified_done}
      Node: {status: verified_done}
      Connection: {status: verified_done}
      SimulationSpec: {status: verified_done}

  backend/core/ir_validator.py:
    status: verified_done
    description: "Structural validation before any compiler: no floating nodes, voltage ratings, I2C pullups"
    tests: "test_rule_engine.py covers validate_ir via integration"
    key_functions:
      validate_ir: {status: verified_done}

  backend/core/config.py:
    status: verified_done
    description: "pydantic-settings — resolves .env from project root via Path(__file__)"
    notes: ".env path fix applied 2026-06-02 (was using CWD, broke when running from backend/)"

# ─── AI LAYER ────────────────────────────────────────────────────────────────

  backend/ai/client.py:
    status: verified_done
    description: "Shared client factory for Anthropic direct + OpenRouter"
    key_functions:
      make_client: {status: verified_done}
      ai_model: {status: verified_done}
    notes: "ANTHROPIC_BASE_URL must omit /v1 — SDK appends /v1/messages itself"

  backend/ai/intent_parser.py:
    status: verified_done
    description: "Prompt → DesignSpec via tool_use forced call"
    tests: "test_ai_layer.py unit tests pass; live tests skipped without API key"
    key_classes:
      IntentParser.parse: {status: verified_done}
      DesignSpec: {status: verified_done}

  backend/ai/circuit_reasoner.py:
    status: verified_done
    description: "DesignSpec → CircuitIR with 3-attempt retry loop"
    tests: "verified by 20-prompt live session"
    key_classes:
      CircuitReasoner.generate: {status: verified_done}
      CircuitGenerationError: {status: verified_done}
    notes: "Handles APIError (no retry), JSONDecodeError, ValidationError — different response for each"

  backend/ai/patcher.py:
    status: verified_done
    description: "Returns only changed fields — NEVER full IR"
    tests: "verified by 5 sequential patches test (v1→v6, no corruption)"
    key_classes:
      CircuitPatcher.patch: {status: verified_done}
      PatchResult.apply_to: {status: verified_done}

  backend/ai/explainer.py:
    status: untested
    description: "Consequential plain-English report — what breaks if component is wrong"
    tests: "no dedicated unit test — called in integration, produces output"
    key_classes:
      ExplanationEngine.explain: {status: untested}
    notes: "Criteria 12 (external review) will validate output quality"

# ─── GENERATORS ──────────────────────────────────────────────────────────────

  backend/generators/netlist/spice.py:
    status: verified_done
    description: "CircuitIR → SPICE netlist. MCU=100Ω load. Floating nodes → 1GΩ tie-down."
    tests: "test_simulation.py — 13 netlist tests, all pass"
    key_classes:
      SpiceNetlistGenerator.generate: {status: verified_done}
    notes: "Single .print directive per analysis (generator changed 2026-06-02)"

  backend/generators/firmware/arduino.py:
    status: verified_done
    description: "CircuitIR → .ino via Jinja2 templates"
    tests: "test_firmware_generator.py — template + arduino-cli compile tests pass"
    key_classes:
      ArduinoFirmwareGenerator.generate: {status: verified_done}
    templates:
      sensor_read.ino.j2: {status: verified_done, compiles: true}
      modbus_master.ino.j2: {status: verified_done, compiles: true, fix: "SoftwareSerial not Serial1"}
      base.ino.j2: {status: verified_done, compiles: true}
      led_blink.ino.j2: {status: verified_done}

  backend/generators/schematic/kicad.py:
    status: untested
    description: "CircuitIR → .kicad_sch with net labels (Phase 1: no wire routing)"
    tests: "no dedicated unit test"
    key_classes:
      KiCadSchematicGenerator.generate: {status: untested}

  backend/generators/bom/compiler.py:
    status: verified_done
    description: "CircuitIR → BOM rows with static pricing from component_constraints.py"
    tests: "test_bom.py — 19 tests, all pass"
    key_classes:
      BOMCompiler.compile: {status: verified_done}

# ─── SIMULATION ──────────────────────────────────────────────────────────────

  backend/simulation/runner.py:
    status: verified_done
    description: "NgspiceRunner — async subprocess, -o outfile capture (Windows fix)"
    tests: "test_simulation.py e2e — voltage divider + RC filter, both pass"
    key_classes:
      NgspiceRunner.run: {status: verified_done}
      NgspiceRunner._run_sync: {status: verified_done}
    notes: |
      CRITICAL: On Windows, ngspice_con.exe writes to console handle not stdout pipe.
      MUST use -o outfile flag. stdout pipe capture returns empty string.
      ngspice path: C:\msys64\ucrt64\bin\ngspice_con.exe (auto-detected by _find_ngspice())

  backend/simulation/parser.py:
    status: verified_done
    description: "DC columnar table + AC multi-table complex magnitude parser"
    tests: "test_simulation.py — 14 parser + grader tests, all pass"
    key_classes:
      SpiceResultParser.parse: {status: verified_done}
      SpiceResultParser._parse_ac_table: {status: verified_done}
    notes: |
      AC parser rewrote 2026-06-02 — handles: separate table per node, pagination,
      complex format (real, imag → strip comma), magnitude = sqrt(re^2 + im^2)

  backend/simulation/grader.py:
    status: verified_done
    description: "Pass/fail at 15% tolerance — DC and AC"
    tests: "test_simulation.py — 7 grader tests, all pass"
    key_classes:
      SimulationGrader.grade: {status: verified_done}

  backend/simulation/monitor.py:
    status: untested
    description: "Per-circuit-type success rate logger → sim_monitor.jsonl"
    tests: "no dedicated unit test"
    key_classes:
      SimulationMonitor.record: {status: untested}
      SimulationMonitor.check_launch_gate: {status: untested}

# ─── VALIDATION ──────────────────────────────────────────────────────────────

  backend/validation/rule_engine.py:
    status: verified_done
    description: "RS-485 termination check, PWM pin validation for Arduino Uno"
    tests: "test_rule_engine.py — 22 tests, all pass"
    key_classes:
      HardwareRuleEngine.run: {status: verified_done}

# ─── API ROUTES ──────────────────────────────────────────────────────────────

  backend/api/routes/auth.py:
    status: verified_done
    description: "register + login (OAuth2 form) + me — bcrypt + JWT"
    tests: "test_auth.py — 17 tests, all pass. Live verified."
    key_functions:
      register: {status: verified_done}
      login: {status: verified_done, note: "OAuth2 form format — not JSON"}
      me: {status: verified_done}
      get_current_user: {status: verified_done}

  backend/api/routes/design.py:
    status: verified_done
    description: "POST /design/generate (10/hour) + GET /design/list"
    tests: "integration — 6+ designs generated live, 100-request load test (0 500s)"
    notes: "REMOVED from __future__ import annotations — caused 422 with slowapi decorator"

  backend/api/routes/patch.py:
    status: verified_done
    description: "POST /design/{id}/patch (20/hour)"
    tests: "integration — 5 sequential patches, v1→v6, no corruption"
    notes: "REMOVED from __future__ import annotations"

  backend/api/routes/simulate.py:
    status: verified_done
    description: "GET /design/{id}/simulation/{job_id} polling + POST start"
    tests: "integration — simulation job polled to completion"
    notes: "REMOVED from __future__ import annotations"

# ─── DATABASE ─────────────────────────────────────────────────────────────────

  backend/db/crud.py:
    status: verified_done
    description: "Async CRUD: save_design, get_design, update_design_ir, record_patch, save_output"
    tests: "integration — designs persist across requests, patches recorded"
    key_functions:
      save_design: {status: verified_done}
      get_design: {status: verified_done}
      update_design_ir: {status: verified_done}
      record_patch: {status: verified_done}
      list_user_designs: {status: verified_done}
      save_output: {status: verified_done}

# ─── TASKS ────────────────────────────────────────────────────────────────────

  backend/tasks/simulation_task.py:
    status: verified_done
    description: "Celery task run_simulation — dispatched from generate, polled via API"
    tests: "integration — RC filter simulation polled to completion"
    notes: "Start worker with --pool=solo on Windows Python 3.13"

# ─── SUMMARY ─────────────────────────────────────────────────────────────────

summary:
  modules_total: 23
  modules_verified_done: 19
  modules_untested: 4
  modules_broken: 0
  modules_not_started: 0
  pct_verified: 82.6
  test_suite: "177 passed, 18 skipped, 0 failed"
  phase1_criteria: "10/12 done — 2 remaining require physical hardware + human"
