# Claude Code Entry Point

This file is auto-loaded by Claude Code at session start.

**Read `AGENTS.md` immediately and follow the bootstrap instructions.**

`AGENTS.md` is the canonical bootstrap shared by all agents (Claude.ai, Claude Code,
GPT, Gemini). Keeping it in one file means every model sees the same instructions.
